name: issue demo
on:
  issues:
    branches: ["main"] 
    types:
      - opened
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: check only
        run: echo "hellow"
    
      
    
