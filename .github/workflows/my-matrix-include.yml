name: self matrix test
on: 
  push:
    branches: ["main"]
jobs:
  build:
    name: building
    strategy: 
      matrix: 
        os: ["ubuntu-latest"]
    runs-on: ${{matrix.os}}
    steps:
      - name: test python
        run: echo $PWD